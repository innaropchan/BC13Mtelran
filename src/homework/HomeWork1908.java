package homework;

public class HomeWork1908 {
    // charAt(int index) - возвращает символ, по указонному индексу строки
    // codePointAt(int index) - возвращает уникодсимвол  по указанному индексу.
    //codePointBefore(int index) - возвращает уникод-символ, который предшествует указанному индексу
    //codePointCount(int beginIndex, int endIndex)
    //compareToIgnoreCase(String str)
    //concat(String str) - объединяет строки, путем добавления одной строки в конец к другой.
    //contains(CharSequence s) - метод возвращает true тогда и только тогда, когда эта строка содержит указанную последовательность значений char.
    //endsWith(String suffix)
    //equals(Object anObject)
    //equalsIgnoreCase(String anotherString)
    //indexOf(int ch)
    //isEmpty()
    //length()
    //replace(char oldChar, char newChar)
    //startsWith(String prefix)
    //substring(int beginIndex)
    //toCharArray()
    //toLowerCase()
    //toUpperCase()
    //trim()
    //
}

